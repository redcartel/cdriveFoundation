## Unit 1 Topics

* The interactive python environment

* python files

* Immutable datatypes: integer, float, string, boolean, None

* Mathematical operations on integers and floats: ```+, -, *, /, **, //, %```

* Expressions are units of python code that produce a value

* assignment to variables

* strings

* joining strings with +, repeating strings with *

* strings are sequences of characters

* definition of booleans, and, or, and not

* calling functions, what arguments are ```functionname(argument1, argument2)``` etc.

* built in functions: int(), float(), str(), round(), ord(), chr(), len(), input()

[https://docs.python.org/3/library/functions.html]

* using input to get user input

* converting user input into ints and floats

* compound expressions, combining expressions with operators, calling functions with expressions as arguments, etc.

* print and "string".format()

* string methods, .upper(), .lower(), .isalpha() 

[https://docs.python.org/3/library/functions.html]
